import { motion } from "framer-motion";
const Contact = () => {
  return (
    <div className="min-h-screen relative">
      <div className="absolute bg-gradient-to-b from-primary-dark via-primary-dark/10 to-primary-dark/0 top-0 h-full w-full"></div>
      <motion.div className="relative"></motion.div>
    </div>
  );
};

export default Contact;
